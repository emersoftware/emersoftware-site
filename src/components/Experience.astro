---
import ExperienceItem from './ExperienceItem.astro';
import { getDictionary } from '../i18n/dictionaries';
import { isSupportedLocale, defaultLocale } from '../i18n/config';
interface Props {
  lang?: string;
}
const { lang: rawLang = 'es' } = Astro.props as Props;
const lang = isSupportedLocale(rawLang) ? rawLang : defaultLocale;
const t = getDictionary(lang);
---

<h2 class="text-left text-lg text-black lg:text-2xl">{t.sectionHeadings.experience}</h2>
<ul class="mt-8 text-sm text-black">
  {
    t.experienceItems.map((item) => (
      <ExperienceItem
        company={item.company}
        date={item.date}
        product={item.product}
        description={item.description}
        link={item.link}
        viewLabel={t.list.view}
      />
    ))
  }
</ul>
